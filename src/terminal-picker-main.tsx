import React from 'react';
import ReactDOM from 'react-dom/client';
import { TerminalPickerWindow } from './TerminalPickerWindow';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TerminalPickerWindow />
  </React.StrictMode>,
);
