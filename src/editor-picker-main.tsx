import React from 'react';
import ReactDOM from 'react-dom/client';
import { EditorPickerWindow } from './EditorPickerWindow';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EditorPickerWindow />
  </React.StrictMode>,
);
